# A template, not a ready-to-upload file: RegisterViaCsvE2ETest replaces every UNIQUE_SUFFIX with
# a per-run value before uploading it.
#
# The xid -- the subject's external identifier, an MRN in a clinical deployment -- is what decides
# whether the row registers anyone. Admin matches an incoming subject by xid and department
# (AccessCodeService -> Status.findByXidAndDepartmentId), so a blank or reused xid in department 1
# is answered with "Existing Subject" and nothing is registered. This fixture used to leave it
# blank, which passed only on a database that had never run this test -- not on a re-run, and not
# on the V2 brownfield database (../PGDATA_v2), which still holds the subject an earlier run
# registered.
#
# Email is deliberately NOT an identity: a household shares one address (a parent and child, a
# married couple), so several subjects legitimately have the same email. It carries the suffix
# only because the test searches by it and wants exactly one hit.
#
# departmentId 1 is the department this suite creates (Admin UC-028: none is seeded any more).
# departments_seq still restarts at 1, so the first department created on a reset stack gets id 1.
#departmentId,firstName,lastName,middleName,dob,email,phone,xid
1,Csv,E2eTestUNIQUE_SUFFIX,,1990-01-15,csv.e2e.test.UNIQUE_SUFFIX@example.org,123-456-7890,UNIQUE_SUFFIX
