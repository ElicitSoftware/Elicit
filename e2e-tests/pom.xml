<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.elicitsoftware</groupId>
    <artifactId>e2e-tests</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Elicit E2E Tests</name>
    <description>
        Cross-application Playwright browser tests driving the Admin (localhost:8081) and Survey
        (localhost:8080) apps against an already-running docker-compose stack. This module starts
        and manages neither app itself. See docs in each app's docs/use_cases/ for the UC-XXX ids
        referenced by these tests. Requires the fhhs service to be up too (it populates real
        survey content) and Playwright's Chromium browser installed locally (run
        `mvn -pl e2e-tests exec:java -e -Dexec.mainClass=com.microsoft.playwright.CLI
        -Dexec.args="install chromium"` once if it isn't already).
    </description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <playwright.version>1.59.0</playwright.version>
        <junit.version>6.1.1</junit.version>
        <surefire-plugin.version>3.5.2</surefire-plugin.version>
        <!-- Opt-in module: skip by default (needs a live docker-compose stack). Run explicitly
             with -DskipTests=false, e.g.: mvn -pl e2e-tests -DskipTests=false test -->
        <skipTests>true</skipTests>

        <!-- Defaults match docker-compose.yml; override with -Dadmin.baseUrl=... etc for CI. -->
        <admin.baseUrl>http://localhost:8081</admin.baseUrl>
        <survey.baseUrl>http://localhost:8080</survey.baseUrl>
        <admin.username>admin</admin.username>
        <admin.password>admin</admin.password>
        <!-- Override with -De2e.headless=false to watch the browser locally. -->
        <e2e.headless>true</e2e.headless>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.microsoft.playwright</groupId>
            <artifactId>playwright</artifactId>
            <version>${playwright.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <skipTests>${skipTests}</skipTests>
                    <includes>
                        <include>**/*E2ETest.java</include>
                    </includes>
                    <systemPropertyVariables>
                        <admin.baseUrl>${admin.baseUrl}</admin.baseUrl>
                        <survey.baseUrl>${survey.baseUrl}</survey.baseUrl>
                        <admin.username>${admin.username}</admin.username>
                        <admin.password>${admin.password}</admin.password>
                        <e2e.headless>${e2e.headless}</e2e.headless>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
